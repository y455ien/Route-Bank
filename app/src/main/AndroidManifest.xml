<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.routebank">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.AdminOptions.ListAllScreen"></activity>
        <activity android:name=".View.AdminOptions.SearchAccountScreen" />
        <activity android:name=".View.AdminOptions.EditAccountScreen" />
        <activity android:name=".View.AdminOptions.CreateAccountScreen" />
        <activity android:name=".View.AdminOptions.AdminEditingScreen" />
        <activity android:name=".View.AccountOptions.TranseferScreen" />
        <activity android:name=".View.AccountOptions.DepositeScreen" />
        <activity android:name=".View.AccountOptions.WithdrawScreen" />
        <activity android:name=".View.AccountOptions.CheckBalanceScreen" />
        <activity android:name=".View.AccountOptions.MainScreen" />
        <activity android:name=".View.Login.AdminCreation" />
        <activity android:name=".View.Login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>